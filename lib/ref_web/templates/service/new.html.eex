<h1>New Service</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.service_path(@conn, :create, @current_user.username)) %>

<span><%= link "Back", to: Routes.service_path(@conn, :index, @current_user.username) %></span>
